plugins {
    // This version contains the internal fixes required for the Gradle 9.0 ecosystem
    id 'com.android.library' version '9.0.0-alpha01' apply false
}

tasks.register('clean', Delete) {
    delete rootProject.buildDir
}
